mixin fiche-produit(imageLink, imageID, title, paragraphe1, title2, paragraphe2)
  article
      // FICHE PRODUIT MODAL BOOTSTRAP
      #modalCBlancPTrois.modal.fade(tabindex='-1', role='dialog', aria-labelledby='myModalLabel')
        .modal-dialog(role='document')
          .modal-content
            .modal-header
              button.close(type='button', data-dismiss='modal', aria-label='Close')
                span(aria-hidden='true') ×
            .modal-body.text-center
              img(src="#{imageLink}#{imageID}")
              div
                h2=title
                p=paragraphe1
                h2=title2
                p=paragraphe2
            .modal-footer
              button.btn.btn-default(type='button', data-dismiss='modal') Close
